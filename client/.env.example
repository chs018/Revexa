# Base URL of the Revexa server (REST + Socket.io). Optional — defaults to
# http://localhost:4000 if unset. Only needs setting if the server isn't
# running on the default port/host, e.g. pointing a deployed client at a
# deployed server.
VITE_API_BASE_URL=http://localhost:4000
